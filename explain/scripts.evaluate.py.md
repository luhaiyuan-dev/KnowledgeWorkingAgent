# `scripts/evaluate.py` 详解

评估脚本从自身位置计算项目根并加入模块搜索路径，因此 README 中的直接文件命令可用。它保存一组最小问题，依次调用真实服务和 `ResponseEvaluator`，打印问题、答案与指标 JSON。问题覆盖知识库引用和工具计算，让开发者修改切分、Embedding 或路由后快速发现回归。

它不是完整 benchmark：样本少，没有黄金答案和延迟统计。扩展时应把评估集放 JSONL，包含期望 route、必须命中的 source、关键事实、用户角色和禁止行为，并在 CI 中设置阈值。测试集不得包含生产客户敏感数据。
