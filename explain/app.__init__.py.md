# `app/__init__.py` 详解

这个文件让 `app` 成为明确的 Python 包，并保存项目版本 `APP_VERSION`。版本集中在包入口的好处是 API、日志、打包脚本都能导入同一个值，避免多个地方各写一份。当前值是 `0.1.0`，代表第一个可运行教学版本。

文件没有业务逻辑，也不在导入时创建模型或连接数据库。Python 包的 `__init__.py` 会在第一次导入时执行，如果在这里做重任务，会导致测试收集、CLI 帮助甚至编辑器补全都变慢。面试中可说明：包入口应轻量、无副作用，只暴露稳定公共信息。
