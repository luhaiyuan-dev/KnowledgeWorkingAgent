# 测试覆盖矩阵与未覆盖风险

现有测试覆盖正常解析、路径越权、切分、Embedding、检索引用、权限、PII、计算器、Memory、路由、服务和 HTTP。尚未在自动测试中调用真实在线 LLM 或 Tavily，这是刻意的：CI 不应依赖付费密钥和不稳定网络。

上线前要在隔离环境增加真实模型契约测试、超时/重试测试、恶意文档注入、超大文件、扫描 PDF、损坏 Office 文件、多租户越权、并发重建索引、Web 内容注入和所有外部写工具的确认/幂等测试。测试不仅证明“能用”，更要证明“不能越权做不该做的事”。
